meta {
  name: Current identity
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/auth/me
  body: none
  auth: inherit
}

tests {
  test("identity is authenticated", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
