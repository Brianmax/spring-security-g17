meta {
  name: Virtual Banking API - Phase One
}

auth {
  mode: none
}

docs {
  End-to-end requests for the phase-one virtual banking API.
  Run the collection in sequence with the Local environment selected.
  Runtime variables capture every resource ID. No authentication is required.
}
