meta {
  name: Get source account
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/v1/accounts/{{sourceAccountId}}
  body: none
  auth: none
}

tests {
  test("source account is returned", function () {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().id).to.equal(bru.getVar("sourceAccountId"));
  });
}
